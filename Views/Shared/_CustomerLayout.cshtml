<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>MyMovies - Booking Tickets</title>
    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Cinzel+Decorative:wght@700&display=swap" rel="stylesheet">

    <style>
        body {
            background-color: #f5f6fa;
        }

        header .navbar-brand {
            font-family: 'Cinzel Decorative', cursive;
            font-size: 1.8rem;
            font-weight: bold;
            color: #ff4757 !important;
            margin-right: 20px;
        }

        .movie-card {
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0px 4px 10px rgba(0,0,0,0.1);
            transition: transform 0.2s ease;
        }

            .movie-card:hover {
                transform: scale(1.03);
            }

        .card-img-top {
            height: 250px;
            object-fit: cover;
        }

        .card-title {
            background-color: #e0e0e0;
            padding: 8px;
            border-radius: 8px;
        }

        footer {
            background-color: #2f3542;
            color: #fff;
            padding: 20px 0;
            text-align: center;
            margin-top: 40px;
        }

        .search-bar {
            max-width: 250px;
        }
    </style>
</head>
<body>

    <!-- Header & Navigation -->
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark px-4">
            <a class="navbar-brand" href="/Customer/Home/Index">MyMovies</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <!-- Navigation links on the left -->
                <ul class="navbar-nav me-auto">
                    <li class="nav-item"><a class="nav-link" href="/Customer/Home/Index">Home</a></li>
                    <li class="nav-item"><a class="nav-link" href="/Customer/Home/Category">Categories</a></li>
                    <li class="nav-item"><a class="nav-link" href="/Customer/Home/Ceima">Cinemas</a></li>
                   
                </ul>

                <!-- Search bar aligned to the right -->
              
            </div>
        </nav>
    </header>

    <!-- Main Section -->
    @RenderBody()

    <!-- Footer -->
    <footer>
        <p>&copy; 2025 MyMovies. All Rights Reserved.</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
